# Roadmap

這份文件說明目前系統的已知限制、未來想探索的方向，以及明確不打算做的事。它是**方向說明**，不是排程表或承諾——沒有時間表，項目會依維護者的時間與優先順序調整。

## 已知限制

以下每一條都對應到目前程式碼的實際狀態，不是空泛的「還有進步空間」：

- **只支援繁體中文、台灣總經場景**。所有 agent 的 prompt 都是全中文寫死（`apps/server/src/prompts/`），資料層的預設新聞來源、市場數據序列（TWSE、FRED 等）也都是為台灣讀者設計。換語言或換市場等於重寫 prompt 與資料層，不是改一個設定值就好。

- **Podcast 語音合成綁定特定 provider**。目前只支援 Gemini TTS 或 Azure Speech 兩選一（`PODCAST_TTS_PROVIDER` 環境變數），沒有本地 / 開源 TTS 的選項。Gemini 路徑有已知的長音檔變調問題，這也是當初加入 Azure 選項的原因；沒有第三個更輕量的選項。

- **OpenAI 相容路徑（自架端點、OpenRouter）尚未對真實端點驗證過，且有一個已知相容性缺口**。程式碼裡（`apps/server/src/agents/providers/openai.ts`）目前寫死用 `response_format: json_schema` 加 `strict: true` 跟模型要結構化輸出。官方 OpenAI API 支援這個參數，但相當多自架推論服務（例如較舊版本的 vLLM、Ollama、LM Studio）與 OpenRouter 上的多數開源模型並不支援 strict JSON schema——實際打過去可能收到 400 錯誤，或是端點忽略這個參數、吐出非 JSON 內容導致解析失敗，而錯誤訊息不會直接告訴你根因是端點不支援這個欄位。目前沒有 fallback（例如偵測失敗後降級成 `json_object` 或整段拿掉 `response_format` 重試）。程式碼裡的註解也明講：這段邏輯還沒有找到一個真的會踩雷的自架端點實際跑過，所以還沒有動手加退路——如果你有這樣的環境並願意幫忙驗證，這會是一個具體且有價值的貢獻。

- **內建新聞來源預設只有 8 個官方網域**。乾淨 clone 之後執行 seed，預設只會啟用「以公開發佈為目的」的官方來源（央行、金管會、行政院、TWSE、聯準會、EIA、白宮等，見 `packages/db/src/source-policy.ts` 的 `OFFICIAL_SOURCE_DOMAINS`）；其餘商業媒體 RSS 來源預設停用，需要自己設定環境變數明確啟用（並自行確認各站的使用條款）。這代表乾淨環境跑起來的新聞覆蓋率會明顯低於維護者自己的環境。

- **目前沒有可公開存取的線上 demo**。要看實際畫面，需要照 [Local Development](docs/operations/local-dev.md) 在本機把整套系統跑起來。

- **跨任務（job）的 LLM 呼叫還沒有節流機制**。單一 job 內有呼叫數上限與並行上限，但如果你寫腳本連續、密集地跑很多份報告或批次評測（例如重跑歷史資料、做 model A/B），目前沒有機制擋下「多個 job 疊加起來」的總請求量——這件事曾經真的讓維護者的 LLM API 專案被 provider 判定為異常流量而降級。批次操作前務必先估算總呼叫數、分批執行。（`POST /api/brief/analyze` 這個 HTTP 入口本身已有限流，見 [部署「放上公網前要知道的事」](docs/operations/deploy.md#放上公網前要知道的事)；但批次腳本是直接呼叫 enqueue 或跑 CLI，不經過這個 HTTP 端點，不受它保護。）

- **部分新聞來源只拿得到標題與轉址連結、讀不到正文**。`news_sources` 裡 `rss_url` 是 Google News 搜尋結果代理的來源（`isGoogleNewsProxySeed` 判準；`packages/db/src/seed.ts` 裡 `google-news-*` 前綴與 `bloomberg-markets`／`wsj-markets`），RSS `<link>` 指向的是 Google 的轉址頁，`news-refresh` 在 feed 層就整批跳過抓正文——這是刻意的取捨，不是待修的 bug。連帶要知道的一件事：`isGoogleNewsProxySeed` 這一個判準同時決定「抓不抓正文」「零正文要不要告警（`ACCEPTED_ZERO_USABLE_NEWS_SLUGS`）」與 `news:health` 把誰當代理來源，所以把某個來源的 feed 換成 `news.google.com`，等於同時關掉它的正文抓取與對應告警；這批來源的則數仍受來源靜默偵測看管，但「沒有正文」對它們是既定狀態、不會亮燈。需要正文請自行以合法方式取得：改用發行商自己的公開 feed（`seed.ts` 裡 cnyes／udn／yahoo-stock 是先例），或自行在 `apps/server/src/news/refresh.ts` 的 `resolveArticleTarget`／`fetchAndStoreBody` 接上 URL 解析。

## 想做的方向

以下是目前看得到、但還沒有人排進行程的方向，列出來是希望有興趣的人可以從這裡找到切入點，不代表維護者一定會做、也不代表順序：

- **市場數據廣度**：目前的持股／部位面向資料集中在台股期貨與融資融券；美股 positioning（例如 CFTC 週報）尚未涵蓋，如果有人想擴展這塊會是一個獨立、範圍明確的資料源整合工作。
- **來源權威分級**：新聞選稿目前只用溫和的權重乘數區分來源，還沒有依「這個來源能不能直接支持數字或政策事實」（官方公告 vs. 媒體轉述 vs. 評論）做分級。如果要做，建議先從少數幾個高確定性主題（例如央行決策、公司法說）做小範圍試點，不要一次覆蓋所有新聞。
- **總經事件的預期差（surprise）追蹤**：目前的敘事只描述「發生了什麼」，還沒有系統性的「實際值 vs. 市場預期（consensus）vs. 前值」比較與事件後的資產反應追蹤。這需要先確認有沒有免費、合法、可長期取得的 consensus 資料源，是否可行本身就值得先做一輪小規模驗證。
- **證據可追溯性的延伸**：目前敘事裡的具名數字已經可以追溯回產生它的分析步驟（claim ledger 機制），但仍有已知的結構性缺口——例如某些被引用的數字沒有正確掛上對應的證據 id、或是分析階段本身沒有把某些有依據的數字抽成可追溯的斷言。這條線適合喜歡做資料正確性 / 可觀測性的人。
- **敘事的連續性延伸到 podcast**：目前跨日的敘事連續性（記得「昨天說過什麼、今天有沒有變化」）只做在文字報告，podcast 逐字稿還沒有反映這個機制。
- **Transcript 工具擴展到更多 podcast 來源**：輔助工具目前只支援 YouTube 連結，擴展到 Apple Podcasts / Spotify 等 podcast feed 是明確可切分的一塊。

如果你想認領以上任何一項，或者有其他在「已知限制」範圍內、能補上具體缺口的想法，歡迎先開 issue 討論再動手。

## 明確不做

以下是刻意的範圍決策，不是還沒空做：

- **不換 agent 框架**、不引入 agentic framework（例如 ADK 之類的），維持目前顯式的 workflow 設計。
- **不把整條 pipeline 改成自主研究迴圈（autonomous research loop）**——每日輸出需要決定性與可測試性，agent 自主決定要查什麼、查多久，跟這個目標衝突。
- **不無限擴大市場數據序列的數量**，除非能對應到明確、可驗證的品質缺口。
- **不做完整的 claim 知識圖譜**——目前只維持三種證據型態（具名數字、事件日期、因果推論），這是刻意收窄的範圍。
- **不整批引入其他系統的主題庫、訂閱制或全分類體系**——需要的分類會依實際需求逐步長出來，不預先建一個大而全的架構。
- **不用單次的亮眼樣本取代連續多日的正式驗證**——這個專案的品質判斷方式是看連續多天的實際產出，不是挑一份漂亮的範例。
- **不做用戶註冊、登入或付費機制。**
- **不做 LINE Bot 整合。**
- **不串接券商 API，不做券商授權或持股管理。**
- **不做圖片 OCR 或截圖辨識功能。**
- **不引入 pgvector 或其他向量檢索式 RAG**——目前的語料規模用 prompt 直接塞入即可，沒有引入向量資料庫的必要性。
- **不把 backend 換成其他程式語言。**
- **不引入額外的規格管理框架。**
